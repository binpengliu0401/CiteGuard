# CiteGuard 讨论与设计决策记录

> 用途：记录项目讨论中问题如何被发现、分析、修正并形成设计决策。
>
> 性质：本地讨论记录，不是当前实现说明，也不作为运行时代码的事实来源。
>
> 更新方式：按讨论日期追加；未实现的结论必须明确标记为“计划”。

## 1. 记录方法

每个主题尽量按以下顺序记录：

1. 最初问题或假设；
2. 暴露问题的反例；
3. 对失败原因的分析；
4. 可以复用的判断原则；
5. 最终接受的设计；
6. 尚未解决的问题；
7. 外部研究和实现参考。

这样记录的目的不是保存聊天原文，而是保留可以复查的推导过程。

---

## 2. 2026-08-25：Planner 应该如何拆分研究问题

### 2.1 讨论起点

用于讨论的总问题是：

> From 2023 to 2026, how have memory systems for LLM agents evolved from
> heuristic external stores to adaptive and policy-learned memory management,
> and what evidence shows how memory writing, retrieval, updating, and
> forgetting affect long-horizon task performance, efficiency, and
> reliability?

这个问题同时包含：

- 2023—2026 的时间约束；
- LLM Agent Memory 这一研究对象；
- 从启发式外部存储到自适应、策略学习型管理的演进；
- 写入、检索、更新和遗忘四种记忆操作；
- 长程任务性能、效率和可靠性三类结果。

### 2.2 最初拆分暴露的问题

最初尝试预先限定子问题数量，再把问题中的主要名词分配进去。这产生了两个
问题：

1. “性能、效率与可靠性”被放进同一个子问题，但它们可能使用不同指标、
   不同实验和不同论文证据；
2. 为了得到预先设定的数量，拆分开始围绕形式上的均匀，而不是围绕答案目标
   和证据边界。

这说明“子问题句子看起来短”并不等于“子问题是原子的”。

### 2.3 失败原因

失败主要来自四个判断错误：

1. 先决定数量，再寻找边界；
2. 按名词数量拆分，而不是按主要答案目标拆分；
3. 没有检查一个子问题是否可能需要不同类型的证据；
4. 把“单篇论文能否回答”误当成原子性的必要条件。

第四点尤其重要。一个原子研究问题仍然可能需要多篇论文联合回答。例如，
“某种机制如何演进”只有一个主要关系目标，但不同阶段通常由不同论文提供
证据。

### 2.4 接受的原子性原则

Planner 不预设子问题数量，而是逐项判断：

1. 一个子问题只能有一个主要答案目标；
2. 如果两个方面可以独立回答，并可能由不同证据验证，应当拆开；
3. 如果比较、演进或因果关系本身就是答案目标，关系两端不应机械拆开；
4. 子问题必须保留原问题中的对象、时间和其他硬约束；
5. 子问题之间应能并行研究，不依赖另一个子问题的研究结论才能理解；
6. 所有子问题联合后必须覆盖原问题，且不能出现实质重复；
7. 不需要拆分的问题可以保持为一个子问题。

因此，原子性描述的是“答案目标是否不可再独立拆分”，而不是“是否可以由
一篇论文回答”。

### 2.5 Planner Eval 的设计边界

Planner Eval 计划检查：

- `Coverage`：原问题的必要方面是否被至少一个子问题覆盖；
- `ConstraintRecall`：对象、方法、时间、场景等硬约束是否保留；
- `SubquestionRedundancy`：子问题之间是否语义重复；
- `Fragmentation`：一个研究方面是否被不必要地拆到多个子问题；
- `Broadness`：一个子问题是否同时拥有多个可以独立研究的目标；
- 可选的依赖图结构是否合法。

这些指标用于评价 Planner 的拆分结果，不评价论文证据质量。

### 2.6 外部参考

- [BREAK/QDMR](https://arxiv.org/abs/2001.11770)：把复杂问题表示为回答它所需的
  有序分解步骤；CiteGuard 借鉴“分解可以被结构化评价”，但不要求开放研究
  问题只有一个标准拆分。
- [BREAK evaluator](https://github.com/allenai/break-evaluator)：提供精确匹配、
  规范化匹配和图结构指标的实现参考。
- [Sentence-BERT](https://arxiv.org/abs/1908.10084)：为固定的语义匹配矩阵提供
  方法基础；阈值必须在人工标注数据上校准，不能直接使用任意余弦阈值。

---

## 3. 2026-08-26：单篇论文如何判断与子问题相关

### 3.1 问题

仅凭标题、关键词或向量相似度，无法区分以下论文：

- 真正回答子问题的论文；
- 只回答一部分的论文；
- 主题相近但只提供背景的论文；
- 关键词相同但研究对象或问题不同的论文。

### 3.2 当前接受的方法

相关性必须相对于一个原子 `SubQuestion` 判断，而不是相对于宽泛的原始问题
判断。Researcher 对每篇候选论文产生以下因素：

| 因素 | 含义 |
| --- | --- |
| `object_match` | 研究对象是否匹配 |
| `problem_match` | 解决的问题是否匹配 |
| `constraint_match` | 时间、方法、场景等约束是否匹配 |
| `evidence_kind` | 摘要是否含有实际方法或发现 |
| `answer_coverage` | 对精确子问题的答案覆盖程度 |
| `supported_aspects` | 摘要可以支持的具体方面 |
| `limitations` | 不能支持的方面和范围限制 |

项目代码根据这些因素确定性地产生：

- `direct`：单篇论文可能完整回答子问题；
- `partial`：提供真实但不完整的答案证据；
- `background`：主题相关，但没有答案型证据；
- `irrelevant`：对象、问题或证据不匹配；
- `unknown`：标题和摘要不足以可靠判断，是弃权而不是第五级相关性。

### 3.3 方法边界

- 当前证据仅限 arXiv 标题和摘要，不能宣称全文级支持；
- 确定性映射保证决策规则稳定，但不能证明 LLM 的因素判断正确；
- 语义可靠性需要双人标注的冻结 Gold 数据集验证；
- 单篇相关性不是多篇论文联合充分性的证明。

### 3.4 外部参考

- [BEIR](https://arxiv.org/abs/2104.08663) 与
  [官方仓库](https://github.com/beir-cellar/beir)：用于把论文是否被检索出来与
  后续证据判断分开，并采用 Recall@K、MRR、nDCG 等检索指标。
- [SciFact](https://arxiv.org/abs/2004.14974) 与
  [官方仓库](https://github.com/allenai/scifact)：把科学文献检索、证据摘要
  选择、支持或反驳标签及 rationale 分开处理。

---

## 4. 2026-08-26：一篇论文不够时如何寻找最小论文集合

### 4.1 问题

一个子问题可能是原子的，但没有任何一篇论文可以单独完整回答。例如，记忆
管理机制的演进是一个主要关系目标，而启发式、自适应和策略学习阶段可能由
不同论文分别提供证据。

需要解决的问题是：

> 如何找到能够联合完整支持答案，同时删除任意一篇后都会变得不完整的最小
> 论文集合？

### 4.2 被否定的简单方案

只计算每篇论文所覆盖字段的并集不够可靠，原因包括：

1. 字段都出现，不代表论文联合后逻辑上能够推出答案；
2. 两篇 `partial` 论文可能重复支持同一部分，而仍然遗漏另一部分；
3. 如果每测试一个论文子集就重新生成答案，模型会缩小答案来适应该子集，
   导致错误的单篇最小解。

因此，搜索过程中必须固定答案目标和待支持的 Claim 列表。

### 4.3 采用 Minimal Evidence Group

采用论文
[Minimal Evidence Group Identification for Claim Verification](https://aclanthology.org/2025.trustnlp-main.8/)
中的 Minimal Evidence Group（MEG）思想。

给定固定 ClaimSet 和论文集合 `G`，有效 MEG 满足：

1. `Sufficiency`：`G` 联合完整支持 ClaimSet；
2. `Minimality`：删除 `G` 中任意论文后都不再完整；
3. `Non-redundancy`：不存在对完整支持没有必要的论文。

### 4.4 与 CiteGuard 的适配

原论文输入的是已经写好的 Claim，而 CiteGuard 输入的是研究问题。因此增加
以下固定链路：

```text
Original Question
    -> Planner: SubQuestion + AnswerTarget
    -> Researcher: per-paper assessments
    -> evidence-bounded structured ClaimSet
    -> MEG: smallest fully supporting paper group
    -> Writer and Verifier
```

`AnswerTarget` 只能由 Planner 产生一次，Researcher 和 MEG 只消费它，不能
重新解释或缩小它。ClaimSet 在 MEG 搜索前冻结；不同论文组合不能各自重写一
套更容易被支持的答案。

### 4.5 搜索方法

1. 使用单篇相关性评估排除 `background`、`irrelevant` 和 `unknown`；
2. 从单篇论文集合开始判断 `FULL/PARTIAL/NONE`；
3. 没有 `FULL` 时，合并部分支持组并增加集合大小；
4. 第一次出现 `FULL` 的大小即当前最少论文数；
5. 删除包含充分真子集的候选组；
6. 保留同样最小的合法证据组，运行时可按固定策略选择一个。

MEG 的组合判断不重复执行论文对象、问题和约束匹配。它复用单篇
`PaperAssessment`，只判断论文证据联合后是否覆盖并支持固定 ClaimSet。

### 4.6 状态映射

```text
supported
    存在至少一个完整的最小证据组

insufficient_evidence
    存在部分有用论文或论文组，但没有完整证据组

no_relevant_sources
    没有论文能够提供答案型证据
```

当前“至少存在一篇 direct 论文才能 supported”的规则将被 MEG 规则替代。
`direct` 只表示值得优先测试的单篇完整候选，不再单独决定最终状态。

### 4.7 示例

子问题：

> 2023—2026 年，LLM Agent 的记忆写入控制如何从固定启发式规则演进到
> 自适应门控和策略学习型管理？

候选论文：

- A：支持早期固定启发式写入规则；
- B：支持上下文相关的自适应写入门控；
- C：支持学习得到的 write/skip/replace 策略；
- D：重复介绍 A，没有提供新的必要证据。

结果：

- `{A}`、`{B}`、`{C}` 均为 `PARTIAL`；
- 任意两篇仍缺少一个必要阶段；
- `{A,B,C}` 为 `FULL`；
- `{A,B,C,D}` 虽然为 `FULL`，但 D 可删除，因此不是 MEG。

最终最小论文集合为 `{A,B,C}`。

### 4.8 与 Planner Eval 的边界

两者都使用“覆盖”和“冗余”词汇，但检查对象不同：

| 模块 | 检查关系 | 失败含义 |
| --- | --- | --- |
| Planner Eval | 原问题方面到子问题 | 问题拆错、遗漏、重复或过度拆分 |
| Paper Assessment | 单篇论文到子问题 | 单篇论文是否提供答案证据 |
| MEG | 固定 ClaimSet 到论文组合 | 证据是否充分、最小且无冗余 |

为避免指标混淆，使用明确命名：

- Planner：`subquestion_coverage`、`subquestion_redundancy`；
- MEG：`evidence_sufficiency`、`evidence_minimality`、
  `evidence_redundancy`。

Planner Eval 保持为离线质量评估，不能在每次研究任务中重复运行。MEG 是
Researcher 的运行时证据选择逻辑。

### 4.9 人工标注试验

第一版继续保持 3 个子问题、每题 4 篇候选论文，共 12 篇：

1. 一题存在单篇 MEG；
2. 一题必须两篇联合；
3. 一题必须三篇联合，并包含一篇冗余或干扰论文。

两位标注者依次确认 AnswerTarget、固定 ClaimSet、单篇标签、组合标签和最终
Gold MEG。组合记录由 12 篇候选论文生成，不需要为每个组合额外搜索论文。

### 4.10 相关实现参考

- [FEVER scorer](https://github.com/sheffieldnlp/fever-scorer)：一个 Claim 可以
  有多个合法的完整证据集合，命中其中一个完整集合即可；适合作为 MEG Eval
  数据结构和完整集合评分参考。
- [EntailmentBank](https://github.com/allenai/entailment_bank)：未来如果需要解释
  多条证据如何推出跨论文 Claim，可参考其证明树；当前阶段不引入证明树。

---

## 5. 已实现的合同方向

以下讨论结论已于 2026-08-26 实现：

1. `ResearchSource.summary` 替换为必填 `abstract`；
2. Researcher 输出结构化 Claim 列表，不输出无来源的自由事实段落；
3. Planner 为每个 SubQuestion 提供固定 `primary_answer_target` 和
   `answer_requirements`；
4. Researcher 保留当前单篇相关性评估；
5. MEG 放在单篇评估与最终 ResearchResult 之间；
6. `supported` 改为由是否存在完整 MEG 决定；
7. Planner Eval、Paper Assessment Eval 和 MEG Eval 分别归因，不混成一个
   总分；
8. 12 条人工数据替换为 3 个 Agent Memory 案例，每案 4 篇候选论文，分别
   覆盖大小为 1、2、3 的 Gold MEG；
9. Eval 增加组合支持宏 F1、完整案例率、基数误差和冗余来源率。

实现保持单一 Researcher Activity，没有增加 Agent、Activity 或数据库表。
MEG 是单篇评估之后、最终 `ResearchResult` 之前的内部阶段。

## 6. 待后续评审的问题

1. 12 条草稿数据完成双人评审后的分歧解决规则；
2. 同样大小的多个 MEG 在运行时如何选择；
3. 论文质量、实验可靠性和冲突证据如何与“最少论文数”分离评价；
4. 摘要证据不足时，何时升级到全文证据；
5. Writer/Verifier 如何扩展当前 Claim 来源绑定，加入范围、因果和模态边界。

---

## 7. 2026-08-26：Agent Memory Eval 草稿复审

### 7.1 暴露的问题

`eval/datasets/researcher_assessment_draft_v0.json` 最初把同一个
2023—2026 原问题手工拆成三个 Researcher 案例。复审发现：

1. `tier-to-agentic` 和 `three-stage-control` 把时间写成 2023—2025，
   丢失了原题的 2026 约束，导致 2026 年的 MemCon 被错误标记为约束不匹配；
2. 三个子问题没有覆盖原题中的 heuristic external stores 起点；
3. 后两个子问题的前两个阶段实质重复，没有增加新的原问题覆盖；
4. 原题的 primary answer target 是演进关系，按 Planner 规则应保留为一个
   子问题，把阶段作为 requirements，而不是按阶段重复拆分；
5. `memory-r1-control__a-mem` 当前 partial 标签缺少摘要级答案覆盖，A-MEM
   并不支持 Memory-R1 的 RL 操作或 Manager/Answer Agent 分工；
6. case 2 和 case 3 的 full 组合及 Gold MEG 依赖按发布时间拼接出的隐式
   演进关系，没有摘要明确陈述跨论文关系；
7. `tier-to-agentic__survey` 的 constraint mismatch 原因没有明确记录；
8. Eval 测试曾有 11 个超过 80 字符的 Python 行。

### 7.2 已接受并实现的处理

1. 时间范围恢复为 2023—2026，survey 与 MemCon 的 constraint 标签和说明
   同步修正；
2. 数据集升级为 schema 5、`0.6.0-draft`；
3. 增加 `evaluation_scope=researcher_meg_mechanism_only`，明确该数据不能作为
   Planner decomposition Gold；
4. 增加 `open_adjudications`，记录 A-MEM 单篇/组合标签和两个跨论文演进
   MEG 的未决裁决；
5. 存在 open adjudication 时禁止把数据提升为 `reviewed` 或 `frozen`；
6. runner 输出携带 evaluation scope 与裁决队列，避免机制夹具的满分被误读
   为语义质量结论；
7. 修复所有 80 字符违规，并保持正式测试通过。

### 7.3 尚未裁决

- A-MEM 在 Memory-R1-specific 案例中应为 `background` 还是
  `irrelevant`；
- 两个演进案例是否能在摘要证据边界下存在合法 full MEG；
- 如果演进关系没有显式来源，是替换候选论文、降低 target，还是取消该
  Gold MEG 案例。

在这些问题解决前，现有 group/MEG 标签只能测试 schema、runner 和指标机制。

---

## 8. 2026-08-26：真实 Planner 输出暴露的 requirement 问题

### 8.1 真实输入与数据边界

真实 no-memory Planner 输入是：

> I keep hearing that LLM agents are moving away from just dumping everything
> into a vector store toward more structured memory management — is that
> actually true, and if so, what's driving it? I'm especially curious whether
> reinforcement learning is being used to teach agents when to update or delete
> old memories.

本项目只允许使用 arXiv 作为研究语料库。Planner 本身不检索论文，但它产生的
每个任务必须能够仅由 arXiv title-and-abstract records 回答；Researcher 工具层
也只暴露 arXiv MCP。

### 8.2 第一轮真实输出的问题

第一轮 Planner 产生三个独立目标：趋势是否存在、转变的驱动力、RL 是否用于
更新或删除记忆。目标拆分总体合理，但 requirements 出现：

- industry blog posts 和 conference talks，超出 Researcher 能力边界；
- `Search for research...` 等检索动作，而不是答案完整性条件。

接受的修正是：

1. arXiv title/abstract 是唯一允许的外部证据语料；
2. requirements 必须是名词短语或陈述式 coverage condition；
3. `search/find/identify/list/describe/provide/retrieve/query/look up` 等动作式
   前缀由 schema 硬门禁拒绝；
4. Prompt 同时给出动作式 requirement 的正反例。

### 8.3 第二轮真实 JSON 的反例

最后一次成功并保存的输出位于：

`tmp/planner_arxiv_only_demo.json`

该输出通过了 arXiv-only 和动作式 requirement 约束，但复审又发现两个更深的
语义问题。

第一，sq-001 的 target 是 `The existence of a trend`，但 requirements 只有：

- 讨论 vector-store 局限的论文；
- 提出结构化记忆架构的论文。

这只能证明存在个例，不能区分方向性趋势与零星方案。`smallest` 被错误解释为
尽量少，而不是满足 target 强度所需的 `smallest sufficient set`。

第二，sq-001 的 vector-store limitations requirement 与 sq-002 的
vector-store challenges requirement 是同一证据需求。该证据本质属于“为什么
转变”的 driver target，不属于“趋势是否存在”的 prevalence target。这会让两个
Researcher 重复检索相同论文并产生重复 Claim。

### 8.4 接受的通用规则

1. requirements 必须是最小但充分的集合；最小化不能删掉支撑 target 强度和
   范围所需的证据；
2. trend、widespread、increasingly、common、emerging 等规模性措辞必须要求
   多个独立 arXiv 来源和时间或分布信号；
3. 如果摘要证据无法支持总体普遍性，target 应降为 documented multi-source
   pattern，而不是承诺更强的 prevalence claim；
4. trend、evolution、comparison、transition 和 causal target 必须包含关系
   本身的 requirement，不能只列关系两端；
5. 每个 evidence need 在整个 decomposition 中只有一个 primary owner；
6. limitations/motivations 属于 why/driver target，frequency/timing/independent
   adoption 属于 trend target；
7. 语义充分性和跨子问题 evidence ownership 当前由 Prompt 和人工 Planner
   Eval 约束，尚无经过校准的确定性语义硬门禁。

### 8.5 条件依赖仍未解决

原问题中的 `is that true, and if so, what's driving it` 表示 sq-002 的最终陈述
依赖 sq-001 是否成立。当前 `SubQuestion` 没有 dependency 字段，Planner 又要求
任务可并行，因此这个逻辑关系会被拍平成并行任务。

当前决定是：研究仍可并行，但后续 Writer 必须保留条件性；依赖图或
dependency-aware synthesis 仍是计划项，不能描述成已实现能力。

### 8.6 当前验证状态

- 25 个 Planner 离线测试通过；完整正式测试为 72 项；
- 早期 no-memory contract 已真实跑通；
- arXiv-only、无动作式 requirement 的中间合同有成功真实输出；
- 最新 sufficiency/ownership 合同尚未获得完整 live 输出；
- 两次 2,500-token 请求返回中途 EOF 的非法 JSON；
- Planner completion ceiling 已提高到 4,000，并在 malformed-output 错误中
  保留 provider `finish_reason`；
- 第一次 4,000-token 请求长时间无响应后被终止，没有保存部分输出，也没有
  创建伪造的 v2 JSON。

因此，当前准确状态是“实现并离线验证；早期合同 live-validated；最新强化合同
等待 live revalidation”。

---

## 9. 2026-08-27：为什么 Writer/Verifier 不等待 Planner Gold

Writer 和 Verifier 消费的是 Researcher 已冻结的 `ResearchClaim`、来源和 MEG
结果，它们解决“证据如何变成结论并被检查”，而 Planner Gold 解决“问题如何
拆分”。两者质量相关，但实现边界可以并行推进。

接受的顺序是：

1. 将当前 `Claim`、`ResearchResult` 和 MEG 输出视为短期冻结边界；
2. 先实现 Writer/Verifier 自己的合成 Gold fixture；
3. 不把 12 条 Researcher 草稿升级为 Gold 作为开工前置条件；
4. 如果 Researcher 输出结构必须变化，先单独完成上游契约修改，再继续下游。

这一决定避免了数据集人工复核阻塞最小闭环，同时也避免 Writer/Verifier 与
Researcher schema 在同一提交中相互追赶。

## 10. 2026-08-28：Writer 与 Verifier 的最小可执行基线

Writer 首版不调用模型，也不自由改写 Claim。它按输入顺序为每个
SubQuestionResult 建立一个 section，并把每个 ResearchClaim 原文转换为一个
带 Claim/source ID 的 statement。这样先建立一个不会扩张证据的安全基线。

Verifier 首版执行确定性硬门禁：检查 section/Claim 覆盖、Claim 作用域、来源
存在性、Claim-source 边、证据状态与原因、重复 Claim 和 Claim 原文。自由文本
变化保守归类为 `unsupported`，不伪装成尚未校准的因果、模态或数值分类。

配套的两个六案例 fixture 分别覆盖 Writer 固定契约与 Writer/Verifier 联合
行为。它们是 synthetic draft，用于实现和回归，不代表语义质量已经校准。

## 11. 2026-08-28：最小 Temporal 闭环如何定义完成

第一条正式 Workflow 固定为：

```text
Planner -> exactly one Researcher -> Writer -> Verifier -> WorkflowResult
```

Planner 如果返回多个子问题，Workflow 在调度 Researcher 前以
`SingleResearcherLimitExceeded` 非重试失败，不能静默丢弃任务。Planner 和
Researcher 的基础设施失败最多尝试三次；无外部副作用的 Writer 与 Verifier
只尝试一次。

Verifier 拒绝不是 Workflow 异常。Workflow 返回完整报告、issues 和
`failed_sub_question_ids`，为后续内容纠错保留可检查状态。内容纠错本身仍未
实现。

验证分为三层：

1. 117 项离线测试覆盖契约、业务分支、失败传播、序列化和 Worker 注册；
2. 本地 Temporal 批准与拒绝路径均实际完成四个 Activity；
3. 瞬时 Researcher 失败在第二次尝试成功，证明基础设施重试没有混入内容
   拒绝。

真实 Temporal 冒烟中的 Planner/Researcher 使用确定性 fixture，以隔离模型和
网络波动；正式 OpenRouter + arXiv 外部全链路仍需单独 live validation。
